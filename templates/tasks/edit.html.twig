{% extends 'base.html.twig' %}

{% block title %}Modifier tâche{% endblock %}

{% block body %}
    <div class="container">
        {{ include('tasks/_delete_form.html.twig') }}

        <h1 class="row justify-content-center mb-5">Modifier {{ task.title }}</h1>

        {{ include('tasks/_form.html.twig', {'button_label': 'Modifier'}) }}
    </div>
{% endblock %}
